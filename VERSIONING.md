# Versioning

Every change to plugin code, behavior, configuration, permissions, or packaging must increment the version in `manifest.json`.

No updated plugin may be released, installed, or reported as complete without:

1. the manifest version incremented;
2. the changed source committed;
3. the package rebuilt or checked;
4. the new version recorded in the handoff.

Current release: `1.0.7`.
